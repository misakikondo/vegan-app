
    <% if current_user.already_liked?(recipe) %>
      <%= link_to 'お気に入り解除', destroy_like_path(recipe.id), method: :delete,remote: true %>
    <% else %>
      <%= link_to 'お気に入り追加', create_like_path(recipe.id), method: :post, remote: true %>
    <% end %>